pipeline {

    agent any

    stages {
        stage('etapa 1') {
            steps {
                echo 'Jelou mundo desde jenkins'
                sh 'echo "desde la terminal"'
            }
        }
    }
    
}